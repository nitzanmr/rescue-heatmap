services:
  web:
    build: .
    image: rescue-heatmap:latest
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped

  # Ready for the next step (backend). Uncomment when the storage layer lands.
  # db:
  #   image: postgis/postgis:17-3.5-alpine
  #   environment:
  #     POSTGRES_DB: rescue
  #     POSTGRES_USER: rescue
  #     POSTGRES_PASSWORD: change-me
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"
# volumes:
#   pgdata:
